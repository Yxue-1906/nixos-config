name: auto-merge-common-config.yml
on:
  push:
    branches: [ common ]

jobs:
  merge-common-config:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
        
      - name: Run script
        run: bash .github/merge-common-and-push.sh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
